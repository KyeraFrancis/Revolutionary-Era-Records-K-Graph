NEO4J_PASSWORD=change-me
# Optional: raises Hugging Face Hub rate limits and download speed.
# The datasets are CC0 and public, so this is not required.
# Create a read token at https://huggingface.co/settings/tokens
HF_TOKEN=
# Optional: for LLM-assisted entity extraction
ANTHROPIC_API_KEY=
